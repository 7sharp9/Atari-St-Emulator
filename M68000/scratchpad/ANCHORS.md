# scratchpad anchors

An index of the `.snap`/`.ram`/`.st` states under `scratchpad/` that a
NOTES.md or a RESUME block has already called out as worth reusing across
passes - not a catalogue of every throwaway per-pass snapshot. If you're
looking for "the settled-world state the 96th-99th passes used" or similar,
start here instead of grepping filenames and old NOTES.md prose.

`scratchpad/` is gitignored (see `.gitignore`'s `M68000/scratchpad/*` +
`!M68000/scratchpad/ANCHORS.md`); this file is explicitly un-ignored so it
survives a commit even though the anchor binaries themselves don't. If an
anchor listed here is missing on a fresh checkout, regenerate it via the
"how to reach it" column, or ask whoever last touched the pass that made it.

| Anchor | State it captures | Made by | Depended on by |
|---|---|---|---|
| `pm69.st` (+ copy at `pm97/pm69.st`) | The raw PowerMonger boot disk image - mission 1, turn 0, nothing played. The root of every other anchor's lineage: every `.snap` below was reached by booting this disk and driving it forward with `run.ps1 window` / the REPL. | 69th pass | Any fresh investigation that needs to replay from turn 0; `run.ps1 window scratchpad/pm69.st` |
| `pm67_ok_pre.snap`/`.ram` | An early, pre-settlement in-game state a few passes before the natural mode-`$7c` corpus was found. | 67th pass | `pm97_map0`'s own lineage (driven forward ~80M steps from here) |
| `pm73_fight.snap`/`.ram` (top-level) | A live 2-army melee in progress (mode `$32` engaged, both sides trading blows) - the "no mode-$32 record" gap the 95th pass's memo flagged, fixed by driving this forward. | 73rd pass (driven further by 95th) | `scratchpad/pm98/repro93.py`, `repro94.py` (comparison set) |
| `pm95/pm73_melee.snap`/`.ram` | `pm73_fight` driven further into full melee - the actual mode-`$32` differential-test anchor (distinct from the top-level `pm73_fight.snap`). | 95th pass | `scratchpad/pm98/repro95.py` (413/413 - the melee/combat gate) |
| `pm74_late.snap` | A later-game state used as a fourth comparison point alongside `pm88_f1`/`pm78_settle`/`pm73_fight` (mission-1-only confirmation, etc.). | 74th pass | `scratchpad/pm98/repro93.py`, `repro94.py` (comparison set) |
| `pm78_settle.snap`/`.ram` (top-level) | A settled-world state with live `$68` formation-follower / settlement records - the base the synthesised mode-`$7c` corpus was built over. | 78th pass | `scratchpad/pm98/repro93.py`, `repro94.py` (comparison set) |
| `pm96/pm78_settle.snap`/`.ram` | Same anchor, copied forward into the 96th pass's own working dir alongside its synthesised corpus script. | 78th pass (copied 96th) | `scratchpad/pm98/repro96.py` (85/85 - the synthesised mode-`$7c` gate) |
| `pm88_f1.snap`/`.ram` (top-level) | An early-game state (frame ~1) used as a baseline/regression comparison point across the dwell-upkeep and movement-mode proofs. | 88th pass | `scratchpad/pm98/repro93.py` (675/675 gate), `repro94.py` (1335/1335 gate) |
| `pm97/pm97_preplay.snap`/`.ram` | `pm67_ok_pre` driven ~80M steps forward with `word[$57fd0]` poked to 0 (forces the mode-`$7c` settlement-heartbeat branch live) - the deterministic pre-play point `pm97_map0` was captured from. Two independent drives from here reproduce byte-identical RAM/snapshots. | 97th pass | `pm97_map0`'s own lineage |
| `pm97/pm97_map0.snap`/`.ram` | **The natural mode-`$7c` anchor.** ~80M-steps-settled: 12 settlements, 19 natural `$7c` markers, 8 live herd ops / 68 markers / ~40 shepherded animals, a live 3-way territory situation. The single most-reused anchor in the corpus. | 97th pass | `scratchpad/pm98/repro97.py` (99/99), `pm99/diff_pm99.py`'s own lineage, `pm113/diff_4342.py` (herd servicer), `pm115/diff_4bc8.py` + `diff_4bc8_kind2.py` (contact reconcile), worked examples in `tools/pm_fsm_ref.py` and `tools/disassemble.py`'s docstring |
| `pm97/pm97_map1.snap`/`.ram` | `pm97_map0` driven further to the one natural state with a *grouped* record hitting the flag-bit-4 group-teardown sub-path - needed because `pm97_map0` itself never naturally exercises it. | 97th/99th pass | `pm99/diff_pm99.py` (1847/1847 - the group-teardown gate) |
| `pm120/k60_iso.snap`/`.ram` | **A second land.** Land 60 (`$580a0 = $68f`, `$5809c = $299a`) built for real through the briefing-OK path, settled 30M steps and stopped at the frame driver `$f898`: camera (45,74), yaw `$f0`, zoom 4, season word 4. 62 settlement-building records, 135 men, new `byte6` 20/22/32. `detcheck 2M` clean. Needs the 120th Timer A and zero-divide fixes. | 120th pass (`pm67_ok_pre` + the poke in `reversing/powermonger/README.md` "Driving a later land"; commands in `pm120/scr_k60.cmds`) | `port/assets_k60/` export, `pm120/k60_iso.json` (order_test input; its own screen is the previous tick's frame: score against the next `$f898` snapshot, SPEC §6 "Scoring a capture") |
| `pm121/k<k>.snap` (33 lands, `k` 0-142) and `k5_s0`/`k5_s3` | Lands built through the briefing-OK poke and settled 30M steps at `$f898` (`k5_s0`/`k5_s3`: land 5 in winter / autumn via `byte[$58146]`). Land 5 has 13 fresh kills (byte6 12), a goods pile, pigeons and a flock at settle. | 121st (`reversing/powermonger/py/build_land.sh <k> [steps] [season]`) | the land census (SPEC §6 "Every category"), the season check, every capture below |
| `pm121/run/<land>_s1..s4.snap` (lands 5, 60, 0, 25; `k5w` = land 5 winter) | The same lands run forward in four 50M-step stretches with no pokes (stretch 1 starts at `pm121/k<land>.snap`, except land 60 at `pm120/k60_iso.snap` and `k5w` at `pm121/k5_s0.snap`; each later stretch at the previous `_sN`); the snapshot after each stretch. Deterministic (re-runs reproduce every hit count). | 121st (`reversing/powermonger/py/runland.sh`, `hits` census in `run/<land>.txt`) | ai.md "Natural runs on later lands"; `reversing/powermonger/py/diff_1623c.py` corpus (`pm121/corpus_1623c/`) |
| `pm121/flip/k60_flip_pre.snap`/`_post.snap` | Land 60 at the garrison defection that follows a `$550e` revolt: pre = at `$25d6` (step 4,509,837 of `run/k60_s3.snap`), post = at its return `$5c7e`. The revolt itself is at step 4,509,545. | 121st | economy.md §3 (how a settlement changes hands) |
| `pm122/end/lose_d2c8.snap` | Land 25 (`run/k25_s4`) after the retire command `$2e` was posted, stopped at the end-of-land verdict `$d2c8` (ratio 0). Continue for the defeat screen; poke `w 57fcc 03b70004` first for the victory branch. | 122nd | strategy.md "How a land ends" |
| `pm122/end/win_after.snap`, `map_cont.snap`, `land1_pick_end.snap`, `land1_built.snap` | After the (forced) victory: main menu with land 0 conquered; the conquest map after Continue Conquest; land 1 picked (at `$11414`, `$580a6` = campaign table entry 1, resource `$b` resident at `$3f428`); land 1 built and running. | 122nd | strategy.md "The campaign"; the 195-entry table |
| `pm122/agents/dissolve/nat2/k60_x1.snap` | Land 60 at `$2776` on the player's captain group (step 94,725,510 after `run/k60_s4`): the natural defeat; the defeat screen follows in ~781k steps. | 122nd | strategy.md "How a land ends" |
| `pm122/prot/run50.snap` | Play Random Land with the uncracked protection question up (`$14e4e = 0`, AI block off). | 122nd | strategy.md "The campaign" (protection check) |
| `pm122/dec/k*_s*_*.snap` | Every natural `$661a` primary decision in the four land runs (25), stopped at `$662a`. | 122nd (`pm122/dec/parse.py`) | strategy.md open threads "The AI on a live enemy" |
| `pm122/agents/{herdop,dissolve,revolt}/` corpora | Natural entry states of `$5cde`, `$2776` and the `$550e` chain (snapshots at the routine entry + `.ram` + callcap JSON). | 122nd | the `diff_5cde.py` / `diff_2776.py` / `diff_revolt.py` gates |
| `pm123/win/m1_s0`, `m1_atk`, `m1_ready`, `m1_win`, `m1_map`, `l1_built` (`.snap`/`.ram`) | **Mission 1 won naturally.** `m1_s0`: campaign land 0 settled 30M steps after the briefing OK (player 26 : enemy 20, ratio 2). `m1_atk`: after the sword-icon + minimap attack on lord 0 (22,45). `m1_ready`: 50M steps later, lord 0 defected, ratio 4. `m1_win`: after RETIRE, victory, main menu (`$3f2a0[0] = 1`). `m1_map`: the conquest map from Continue Conquest. `l1_built`: land 1 built and running at `$f898`. Two runs byte-identical. | 123rd (`reversing/powermonger/py/drive_win.sh`, from `pm67_ok_pre.snap`) | strategy.md "The player's commands", "How a land ends"; the start state for following the player's icon orders |
| `pm123/diplo/offer_a_end`, `t1_end`, `t2_end`, `t2_after60m`, `brk_end` (`.snap`) | Land 60 (`pm121/run/k60_s2`) with the player's envoy (order `$1e`, `w 5801c 011e2d15`) sent to side 3's settlement (45,21): dispatched; arrival forced (lead mode `$76`) with no tribute (refused) and with one goods unit (sides 1 and 3 allied, `$5810c`/`$580cc` = `$0a`); 60M steps later; the alliance broken by contact. | 123rd (subagent; `diplo/*.cmds`, `REPORT.md`) | strategy.md "Diplomacy" |
| `pm124/<order>/end.snap`/`.ram` (`o02`..`o20`, `o0ecap`, `ctl`, `ctl25`) | Mission 1 (`pm123/win/m1_s0`) after one player order each, plus no-order controls at 3M / 25M steps; `o0ecap` has the player's town poked to a capital; `fooddrain/` the `watch $516e4` over 25M. | 124th (`reversing/powermonger/py/order_run.sh`, byte-identical rerun) | strategy.md "What each order does" |
| `pm124/conquest/cap/` (+ `out/`, `win*/`, `spy*/`, `ctl*/`) | 169 natural `$4f68` entry states from `pm123/win/m1_atk` (`capture_hits.py`, `w4f68.json`), the win / spy / control runs with `hits` and `watch` logs, and `REPORT.md`. | 124th (subagent; `BRIEF.md`) | `reversing/powermonger/py/diff_4f68.py` (1804/1804) |
| `pm121/cap/<view>_<i>.snap`/`.ram`/`.json` | Runs of consecutive `$f898` frames over the new categories (dead men, flock, pigeon, leader's base, goods, winter snow, autumn rain, a fight with an arrow, boats, a building going up). Score state i against frame i+1. | 121st (`reversing/powermonger/py/capture.sh`, `dump_frame.py`) | `reversing/powermonger/py/score.fsx`, `pm121/allpairs.txt` (27 pairs, all 100.00%) |
| `pop/repro.snap`, `pop/game_start.snap`, `pop/g90.snap` | **Populous**, conquest world 0 GENESIS, first gameplay frame (frame 285) and 190 frames on. `repro.snap` is the exact output of `reversing/populous/drive.txt` from cold boot (two runs byte-identical); `game_start.snap` reached the same frame by a longer click path (173 RAM bytes differ) and is the one the verifiers load; `g90` is 190 frames after it. Needs `--disk-a scratchpad/pop/pop_auto.st`. | populous pass (`reversing/populous/README.md` "Drive recipe") | every `reversing/populous/py/` verifier (`ai_diff.py`, `verify_gen.py`, `verify_cmd.py`, `capframes.py`) |
| `pop/late1..late4.snap` | Populous GENESIS run +60M..+240M steps from `repro` with no input (frames 835..2161): the computer levels its south-east corner and banks 7485 mana. | populous pass | terrain/AI live-behaviour checks |
| `pop/agents/graphics/*_pre.snap`/`_post.snap` | 12 Populous frames (scrolled views, a selected walker) captured one frame apart for renderer scoring. | populous pass | `reversing/populous/py/pop_render.py --truth` (64000/64000 each) |
| `pop/drive/A.snap` | Populous `game_start` + one minimap click: view (6,12), your town in view, pointer at (58,12). The start point for UI-driven Populous work. | Populous pass 2 (`reversing/populous/py/verify_drive.py`) | `verify_drive.py` (17/17), `py/powers/cast.py`, `knight_scn.py` |
| `pop/walker/snaps/near.snap`, `front.snap`, `gather1.snap`, `fight1.snap` | Populous: `late4` + mode_magnet (`near`, leader 12 at (8,20), frame 2212), then UI clicks only (magnet walk + five bridging raises) until the human leader stands next to the south-west evil towns (`front`, frame 2660); then the gather / fight icon (frame 2665). The only states with human walkers in contact with the enemy. Rebuilt on macOS from the committed recipe (the Windows originals were at frame 2731). | Populous pass 2 (`py/walker/campaign.py`, `campaign2.py`, `mkmode.py <front> mode_gather\|mode_fight <out>`) | `py/walker/capcalls.py` + `livecheck.py` (8000/8000 decisions, on the old states); `py/powers/live.py fight1.snap 1500 fight\|resolve` (take-overs 7/7) |
| `pop/powers/snaps/<power>_entry/_exit/_after.snap`, `kbridge_after.snap` | Populous: each power cast through the UI from `drive/A` (mana poked 200000), at the routine's entry, its exit and 12 frames on; `kbridge_after` = a knight of str 600 with a land corridor to the enemy. | Populous pass 2 (`py/powers/cast.py`, `knight_scn.py`) | `py/powers/live.py` (133/133, 18/18, 3/3) |
| `pop/ai/cg1.snap`, `cg2.snap` | Populous custom game set up through menus only: evil rating 1, all powers, reaction 1, human idle (`cg1`); plus ATARI VS ATARI (`cg2`). | Populous pass 2 (`py/ai/s2_custom.py`, `s3_ava.py`) | `py/ai/cmdlog.py`, `livecheck.py`, `join.py` (1854/1854, 2766/2766; logs in `pop/ai/live/`) |
| `pop/ai/F0.snap`, `M0.snap`, `M1.snap` | Populous endgame powers: `cg1` with evil mana poked to 90000 (flood x2 at frames 316/317); run A frame 1065 with the same poke (armageddon at 1066, GAME LOST at 1506); `M1` = M0 at frame 1067, brawl under way. | Populous pass 3 (`py/ai/s5_endpow.py`; M1 by running M0 to the third `$db4c`) | `livecheck.py`/`join.py F\|M` (111/111, 169/169; logs `pop/ai/live/F_*`, `M_*`), `endgame/brawlcheck.py` on `pop/endgame/brawl/M460.bin` (441/441), `fightcheck.py M1.snap` (12/12) |
| `pop/endgame/<tag>_entry/_score.snap`, `win_next_entry.snap`, `*_brief.snap`, `boot_<mode>_*.snap` | Populous end states at `$1c858` entry and click wait (win, lose, Armageddon loss/win, world 2470 win, surrender, custom loss), the next-world entry, the briefings, and the three title-menu boots. | Populous pass 2 (`py/endgame/`) | `score_diff.py`, `real_scores.py`, `next_diff.py`, `ui_names.py`, `boot_check.py` |
| `pop/systems/spawn.snap`, `tick.snap` | Populous GENESIS at frame $1000, stopped at the trail spawn `$13372` (335M steps after `late4`); `tick` = frame $e78 on the way. | Populous pass 2 (`py/systems/trailrun.py`) | `trail_diff.py` (400/400 + 400/400), `trailrun.py` (394/394 live frames) |
| `pop/swamp208/p_cg2.snap`, `p_spawn.snap` (+ `.after`) | POKED: Populous stopped at the pop-208 swamp spawn `$13372` from `$e8a0`, free slots given str 1 (the `.after` file restores them); from `ai/cg2.snap` (frame 464) and `systems/spawn.snap` (frame 4112). | Populous pass 3 (`py/systems/swamp208.py poke`) | `trailrun.py` with TRAIL_* env (400/400, 237/238) |
